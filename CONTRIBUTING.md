# 문서 기여 가이드

## 목적

이 저장소는 개발자를 위한 구현 명세가 아니라, AppBack을 모르는 기획자·운영자·협업자가
서비스를 이해하는 문서다. 변경할 때는 독자가 코드 저장소 없이도 의미를 알 수 있어야 한다.

## 작성 원칙

- 기능 이름보다 사용자가 무엇을 할 수 있는지 먼저 설명한다.
- 내부 키와 테이블 이름보다 화면 이름과 상태 의미를 사용한다.
- 현재 기능, 설정형 기능, 호환 기능과 계획을 명시적으로 구분한다.
- 보상액, 인원, 날짜와 버전은 변경 가능성을 확인하고 근거를 적는다.
- `항상`, `즉시`, `보장` 같은 표현은 실제 정책 근거가 있을 때만 사용한다.
- 다른 서비스나 제휴사의 내부 구현을 추측하지 않는다.
- 토큰, 비밀번호, 내부 IP, 개인 식별값과 공급 원가를 기록하지 않는다.

## 변경 절차

1. `appback-hub/docs/README.md`에서 현재 원문을 찾는다.
2. 기능 문서가 현재 계약인지 계획인지 확인한다.
3. 이 저장소의 주제 문서와 `docs/00-feature-status.md`를 수정한다.
4. `sources/SOURCE_MAP.md`에 근거와 확인일을 갱신한다.
5. 링크와 Markdown 형식을 검사한다.
6. 변경 이유가 드러나는 커밋을 남긴다.

## 리뷰 질문

- AppBack을 처음 보는 사람이 약어 설명 없이 이해할 수 있는가?
- 사용자가 보는 흐름과 운영자의 책임이 구분되는가?
- 외부 제공사와 Hub의 책임을 뒤섞지 않았는가?
- 계획을 현재 제공 중인 기능처럼 썼는가?
- 구버전 호환 사항을 현재 권장 흐름처럼 썼는가?
- 개인정보나 내부 운영 비밀이 포함됐는가?
- 원문이 없어져도 서비스 개념을 이해할 수 있는가?
